---
import '@fontsource-variable/outfit'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'

import PostHog from '../components/Posthog.astro'

interface Props {
  title: string
}

const { title } = Astro.props
---

<!doctype html>
<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta
      name='description'
      content=`+12 years of experience being completely dedicated and passionate about crafting solid and scalable web products with great user interfaces.`
    />
    <meta name='viewport' content='width=device-width' />
    <link rel='icon' type='image/icon' href='/favicon.ico' />
    <meta name='generator' content={Astro.generator} />
    <title>{title}</title>
    <PostHog />
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <style is:global>
      html,
      body {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
        overflow-x: hidden;
        background-color: rgba(18, 18, 18, 1);
        color: rgba(255, 255, 255, 0.6);
        font-family: 'Outfit Variable', sans-serif;
        -webkit-font-smoothing: antialiased;
      }
      a,
      button {
        all: unset;
        cursor: pointer;
      }

      h1,
      h2,
      h3,
      h4 {
        margin: 0;
      }

      h1 {
        font-size: 48px;
      }

      h2 {
        font-size: 38px;
      }

      h3 {
        font-size: 32px;
      }

      p {
        all: unset;
        font-size: 20px;
        margin-bottom: 16px;
      }

      ul,
      ol {
        list-style: none;
        padding: 0;
        margin: 0;
      }

      @media (max-width: 640px) {
        .responsive-content {
          padding: 0px 16px;
        }
      }

      @media (min-width: 640px) {
        .responsive-content {
<<<<<<< Updated upstream
          max-width: 640px;
          padding: 0px 24px;
=======
          max-width: 560px;
>>>>>>> Stashed changes
        }
      }

      @media (min-width: 768px) {
        .responsive-content {
          max-width: 640px;
        }
      }

      @media (min-width: 1024px) {
        .responsive-content {
          max-width: 768px;
        }
      }

      @media (min-width: 1172px) {
        .responsive-content {
          max-width: 1024px;
        }
      }
    </style>
  </body>
</html>
